{% extends 'base.html.twig' %}

{% block title %}Discount{% endblock %}

{% block body %}
{% for message in app.flashes('notice') %}
        <div class="alert alert-success" role="alert">
            {{ message }}
        </div>
    {% endfor %}
    <h1>Discount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discount.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ discount.name }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ discount.rate }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ discount.createdAt ? discount.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ discount.updatedAt ? discount.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_discount_index') }}">back to list</a>

    <a href="{{ path('app_back_discount_edit', {'id': discount.id}) }}">edit</a>

    {{ include('back/discount/_delete_form.html.twig') }}
{% endblock %}
