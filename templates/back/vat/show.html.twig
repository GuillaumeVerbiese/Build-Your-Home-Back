{% extends 'base.html.twig' %}

{% block title %}TVA{% endblock %}

{% block body %}
{% for message in app.flashes('notice') %}
        <div class="alert alert-success" role="alert">
            {{ message }}
        </div>
    {% endfor %}
    <h1>TVA</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vat.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ vat.name }}</td>
            </tr>
            <tr>
                <th>Taux</th>
                <td>{{ vat.rate }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ vat.createdAt ? vat.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ vat.updatedAt ? vat.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_vat_index') }}" class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('app_back_vat_edit', {'id': vat.id}) }}" class="btn btn-warning">Modifier</a>

    {{ include('back/vat/_delete_form.html.twig') }}
{% endblock %}
