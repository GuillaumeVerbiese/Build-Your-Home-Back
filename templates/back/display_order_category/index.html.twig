{% extends 'base.html.twig' %}

{% block title %}Modifier le Article Order{% endblock %}

{% block body %}

{% for message in app.flashes('doublon') %}
        <div class="alert alert-danger" role="alert">
            {{ message }}
        </div>
    {% endfor %}
<form name="category_displayOrder" method="post">
    <div id="category_displayOrder" novalidate="novalidate">
        <div>
        {% for i in 1..10 %}
        
            <label> Affichage n°{{i}}</label>
            <select type="select" name="{{i}}" required="required">
            <option value="{{ categoryOrderbyDisplay[i].name }}">{{ categoryOrderbyDisplay[i].name }}</option>
            {% for category in categories %}
                <option value="{{category.name}}">{{category.name}}</option>
            {% endfor %}
            
            </select>
        {% endfor %}
        </div>
    </div>
    <button class="btn">Valider</button>
</form>
        {% endblock %}