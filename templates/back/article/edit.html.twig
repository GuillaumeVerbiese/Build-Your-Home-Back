{% extends 'base.html.twig' %}

{% block title %}Editer un Article{% endblock %}

{% block body %}
    <h1>Editer un Article</h1>

    {{ include('back/article/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_back_article_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('back/article/_delete_form.html.twig') }}
{% endblock %}
