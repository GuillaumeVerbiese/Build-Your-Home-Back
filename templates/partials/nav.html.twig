<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

    <div class="container-fluid">
        <a href="{{ path('app_login')}}" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none header-logo">
            <span class="header-logo__title">Built your home</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_login')}}">LOGIN</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login')}}"> LOGIN</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login')}}">LOGIN</a>
                </li>
            </ul>
        </div>

     </div>
</nav>